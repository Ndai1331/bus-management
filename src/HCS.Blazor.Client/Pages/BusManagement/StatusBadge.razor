<span class="badge @(Active ? "text-bg-success" : "text-bg-secondary")">@(Text ?? (Active ? "Hoạt động" : "Ngừng"))</span>

@code {
    [Parameter] public bool Active { get; set; }
    [Parameter] public string? Text { get; set; }

}
